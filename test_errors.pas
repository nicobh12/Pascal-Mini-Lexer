program BuggyProgram;

const
  limit = 100
  rate = 2.5

var
  x, y : integer
  msg : string;

begin
  x := limit 10
  msg := 'hello;
  if x > 0
    y := x * rate;
  for i := 1 to limit
    writeln(i)
end
